extends layout

block content
  h1= error.status
  p= error.message
  if error.handleType === "session"
    p
      a(href=`ressession:///${error.sessionId}`) Try joining session in Resonite.
  if error.stack
    h2 Stack Trace Info
    pre #{ error.stack }
